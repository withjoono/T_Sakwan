# 사관 1차 합불 예측 — 환산점수 변환 설계서

> 작성 2026-07-29 (2027학년도 1차시험 D-3)
> 대상: `/1cha` 랜딩 + `mogo-backend` 신규 모듈
> 관련: [Mogo_연동_설계서.md](./Mogo_연동_설계서.md)

---

## 1. 정정된 전제

초기 구현은 **원점수 총점 300점을 학교별 컷과 직접 비교**하는 구조였다. 이는 틀렸다.

1차 합격자 선발은 원점수가 아니라 **환산점수(표준점수)** 기준이다. 따라서 학생이 입력한 가채점
원점수를 표준점수로 변환하는 단계가 반드시 선행되어야 한다.

### 1.1 "작년컷"의 실체

두 가지가 섞여 있어 혼동이 생긴다. 구분하면:

| 항목 | 공개 여부 |
|---|---|
| **본인 1차 성적(환산점수)** | **각 학교가 공개함** — 국간사 '25.8.1~8.5, 공사 '24.8.2~8.5 성적확인 기간 운영 |
| **모집단위별 합격 커트라인** | **미공개** — 군 당국이 공식 발표하지 않음 |

즉 수험생은 자기 환산점수를 알기 때문에 커뮤니티·학원가에 컷이 돌지만, 그건 제보 기반
추정치이지 학교 발표값이 아니다. 서비스 문구는 "추정 합격선"을 유지해야 한다.

---

## 2. 학교별 1차 성적 산출 방식 (모집요강 원문 근거)

### 2.1 국군간호사관학교 (2026학년도 모집요강 p.13)

> **선발기준: 모집단위별 1차시험 총점(과목별 표준점수 합산점수) 순 선발**

- 국어·영어 표준점수 = `20 × (원점수 − 평균) / 표준편차 + 100`
- 수학 표준점수 = 공통과목 표준점수 + 선택과목 표준점수 + 100
  - 공통 = `20 × (공통원점수 − 공통평균) / 공통표준편차 × 0.74` (배점비율 74)
  - 선택 = `20 × (선택조정점수 − 전체 선택조정점수 평균) / 전체 선택조정점수 표준편차 × 0.26` (배점비율 26)
  - 선택과목 조정점수 = `(수험생 선택과목 원점수 − 선택과목 응시자 원점수 평균) / 선택과목 응시자 원점수 표준편차 × 선택과목 응시자들의 공통과목 원점수 표준편차 + 선택과목 응시자들의 공통과목 원점수 평균`
- 고정값: **평균 100, 표준편차 20** (수능과 동일)
- 합격인원: 모집정원 기준 **여자 4배수, 남자 8배수**

### 2.2 공군사관학교 (2025학년도 모집요강)

> **선발기준: 모집단위별 성적(과목별 표준점수 합산점수) 순 선발**
> 과목별 표준점수 산출방식은 수능 산출방식과 동일, 합산 후 소수점 셋째 자리 반올림하여 둘째 자리까지 반영

- 모집단위별(성별/계열별) 성적순 선발, 선발배수 4/6/8/10배수 (모집단위별 상이)
- 국간사와 동일한 표준점수 합산 척도

### 2.3 육군사관학교 (2026학년도 모집요강 p.12~13)

육사만 **척도가 다르다.**

- 국어·영어·수학 표준점수 산출식은 국간사와 동일 (평균 100, 표준편차 20)
- 단, 선발 기준은 표준점수 합산이 아니라 **반영 기본점수(50점 만점)**
  - `Σ (과목별 개인취득 표준점수 / 과목별 최고 표준점수) × N점`
  - 소수점 넷째 자리에서 반올림, 셋째 자리까지 반영
- 동점자 발생 시 표준점수 총점 순
- 선발: **남자 계열별 5배수, 여자 계열별 8배수** (계열별·성별 구분)

> ⚠ **미확인**: 위 식의 과목별 배점 `N`이 모집요강 PDF에서 수식 폰트로 깨져 추출되지 않았다.
> 3과목 균등이면 각 16.667점이지만 확정 전 검증 필요. → §9 참조

### 2.4 해군사관학교

모집요강 원문 미확보. 공사·국간사와 동일한 표준점수 합산 방식으로 **가정**하고 진행하되,
원문 확보 후 확정한다. → §9 참조

### 2.5 요약

| 학교 | 1차 선발 척도 | 모집단위 | 선발배수 |
|---|---|---|---|
| 국간사 | 표준점수 합산 | 계열×성별 | 여 4배수 / 남 8배수 |
| 공사 | 표준점수 합산 | 계열×성별 | 4~10배수 |
| 육사 | **반영 기본점수 50점 만점** | 계열×성별 | 남 5배수 / 여 8배수 |
| 해사 | 표준점수 합산(가정) | 계열×성별 | 미확인 |

**핵심 시사점 2가지**
1. 육사와 나머지 학교는 **점수 스케일이 다르다.** 하나의 `총점` 필드로 4개 학교를 비교할 수 없다.
2. 선발은 **계열(인문/자연) × 성별** 모집단위별로 따로 이뤄진다. 컷도 모집단위마다 다르다.
   현재 UI는 학교만 받고 계열·성별을 받지 않는다 → 반드시 추가해야 한다.

---

## 3. 핵심 난점 — 표준점수는 응시자 분포에 의존한다

표준점수 = `20 × (원점수 − 평균) / 표준편차 + 100` 에서 **평균·표준편차는 그 해 전체 응시자의
과목별 원점수 통계**다. 시험 당일 가채점 시점에는 아무도 이 값을 모른다.

즉 **원점수만으로는 환산점수를 계산할 수 없다.** 학교 성적발표(시험 약 5일 후)까지 기다려야 한다.

### 3.1 이것이 오히려 이 서비스의 해자다

우리가 모으는 크라우드 제출 데이터가 곧 **평균·표준편차 추정치의 원천**이다.
제출이 쌓일수록 모수 추정이 정확해지고, 환산점수도 실제값에 수렴한다.
경쟁 서비스가 흉내 내기 어려운 지점이며, "빨리 많이 모을수록 정확해진다"는 성장 구조가 된다.

단, 자발적 제출은 상위권 편향이 있다 → §6 보정 참조.

---

## 4. 변환 파이프라인

```
[학생 입력]                    [서버 추정]                      [판정]
과목별 원점수      ──┐
계열(인문/자연)      │    ①모집단위별 원점수 분포 집계
성별                 ├──▶  (누적 제출 → 과목별 μ, σ, max)
수학 선택과목        │              │
                   ─┘              ▼
                          ②표준점수 변환
                            국·영: 20(x−μ)/σ + 100
                            수학: 공통 0.74 + 선택 0.26 + 100
                                   │
                                   ▼
                          ③학교별 척도로 환산
                            공사·국간사·해사: 표준점수 합산
                            육사: Σ(개인표준/최고표준)×N (50점 만점)
                                   │
                                   ▼
                          ④모집단위별 추정 컷과 비교 → 합불 확률
```

### 4.1 단계별 상세

**① 분포 추정**
- 모집단위(학교 무관, 계열×성별)별로 과목별 원점수의 `n, μ, σ, max` 집계
- 표본 부족 구간은 예년 파라미터(SEED)와 블렌딩: `w = min(n / N₀, 1)`
- 수학 선택과목은 과목별(확통/미적/기하) 하위 집계 필요 (조정점수 산출에 사용)

**② 표준점수 변환**
- 모집요강 원문 수식 그대로 구현 (§2.1)
- 선택과목 조정점수는 선택과목 응시자군의 공통과목 통계가 필요 →
  제출 데이터에 `수학 선택과목` 필드가 없으면 계산 불가. **입력 필수 항목**이다.

**③ 학교별 환산**
- 표준점수 합산 계열: 3과목 표준점수 단순 합
- 육사: 과목별 최고 표준점수가 필요 → 표본 최고 원점수로 추정.
  표본이 적으면 최고점이 과소추정되어 육사 점수가 **과대**계산되므로,
  최고 표준점수는 표본 최댓값이 아니라 상위 분위수 기반 추정치를 쓴다 (§6).

**④ 판정**
- 모집단위별 추정 컷 대비 차이 → 로지스틱 확률
- 표본이 적을수록 σ를 키워 확률을 50%로 수렴시킴 (과신 방지)

---

## 5. 입력 사양 변경

현재 `/1cha`는 `지망학교 + 총점(또는 과목별)` 만 받는다. 아래로 확장한다.

| 필드 | 필수 | 비고 |
|---|---|---|
| 지망 학교 | ✅ | 기존 |
| **계열** (인문/자연) | ✅ | **신규** — 모집단위 결정 |
| **성별** | ✅ | **신규** — 모집단위 결정, 선발배수 상이 |
| 국어 원점수 | ✅ | 기존 과목별 입력 재사용 |
| 영어 원점수 | ✅ | 〃 |
| 수학 공통 원점수 | ✅ | **분리** — 수학 I·II |
| **수학 선택과목** (확통/미적/기하) | ✅ | **신규** — 조정점수 산출에 필수 |
| 수학 선택 원점수 | ✅ | **신규** |

> **총점만 입력 모드는 폐기했다.** 총점으로는 표준점수를 계산할 수 없어 결과가 의미를 갖지 못한다.
> 입력 항목이 늘어 이탈이 우려되지만, 틀린 답을 빠르게 주는 것보다 낫다는 판단.

---

## 6. 표본 편향 보정

| 편향 | 영향 | 보정 |
|---|---|---|
| 상위권 자발 제출 편향 | μ 과대추정 → 내 표준점수 과소 | 예년 μ와 블렌딩, 보정폭 상한 |
| 표본 최댓값 과소 | 육사 최고표준점수 과소 → 점수 과대 | 상위 분위수 외삽 사용 |
| 특정 학교 지망자 쏠림 | 모집단위 분포 왜곡 | 분포는 학교 무관 계열×성별로만 집계 |

신뢰도 표기 정책:

| 표본 n | 표기 | 결과 노출 |
|---|---|---|
| < 30 | "표본 부족 · 예년 기준 위주" | 확률 대신 구간 표기 권장 |
| 30~299 | "표본 반영 중" | 확률 + 오차 안내 |
| ≥ 300 | "올해 제출 데이터 충분" | 확률 그대로 |

---

## 7. 백엔드 설계 (`mogo-backend`)

Firestore 직접 쓰기는 폐기하고 `mogo-backend`(NestJS + Prisma + Cloud SQL)로 통일한다.
기존 `src/saagwan-prediction/`(학교별예측)과 별도로 익명 캠페인용 모듈을 둔다.

### 7.1 Prisma 모델

```prisma
// 1차 가채점 익명 제출 — 분포 추정의 원천
model OnechaSubmission {
  id           Int      @id @default(autoincrement())
  anonId       String   @map("anon_id") @db.VarChar(64)   // 단말 UUID, 1인 1제출
  year         Int      @default(2027)                     // 학년도
  track        String   @db.VarChar(10)                    // 인문 | 자연
  gender       String   @db.VarChar(10)                    // 남 | 여
  targetSchool String   @map("target_school") @db.VarChar(50)
  korRaw       Int      @map("kor_raw")
  engRaw       Int      @map("eng_raw")
  mathComRaw   Int      @map("math_com_raw")               // 수학 공통
  mathSelName  String   @map("math_sel_name") @db.VarChar(20) // 확률과통계|미적분|기하
  mathSelRaw   Int      @map("math_sel_raw")
  createdAt    DateTime @default(now()) @map("created_at")

  @@unique([anonId, year])
  @@index([year, track, gender])
  @@map("mg_onecha_submissions")
}

// 연락처 리드 (상세 결과 게이트)
model OnechaLead {
  id        Int      @id @default(autoincrement())
  anonId    String   @map("anon_id") @db.VarChar(64)
  name      String   @db.VarChar(50)
  phone     String   @db.VarChar(20)
  year      Int      @default(2027)
  target    String   @db.VarChar(50)
  segment   String   @db.VarChar(20)
  consent   Boolean  @default(false)
  source    String   @default("1cha") @db.VarChar(30)
  createdAt DateTime @default(now()) @map("created_at")

  @@index([year, createdAt])
  @@map("mg_onecha_leads")
}
```

### 7.2 기존 컷 테이블 확장

현재 `mg_saagwan_cutoffs`는 `(school, track, year)` 유니크에 `cutRaw`(원점수) 하나뿐이다.
환산점수 컷을 담으려면 아래가 필요하다.

- `gender` 컬럼 추가 → 유니크 키를 `(school, track, gender, year)`로 변경
- `cutScaled` 컬럼 추가 (환산점수 컷). `cutRaw`는 하위호환 위해 유지
- `scale` 컬럼 추가: `standard_sum`(표준점수 합산) | `kma_50`(육사 50점 만점)

> 현재 이 테이블은 PLACEHOLDER 5행뿐이므로 마이그레이션 리스크는 낮다.

### 7.3 엔드포인트 (`/api/tsagwan/1cha`)

| 메서드 | 경로 | 설명 |
|---|---|---|
| POST | `/submissions` | 익명 제출. `anonId+year` 유니크로 중복 방지 |
| GET | `/estimate` | 제출값 기준 환산점수 + 모집단위 분포 + 학교별 합불 판정 |
| GET | `/summary` | 분포 히스토그램용 집계 (버킷 카운트) |
| POST | `/leads` | 연락처 리드 저장 (consent 필수 검증) |

- 환산·판정 로직은 **전부 서버**에 둔다. 클라이언트에 두면 조작 가능하고 컷이 노출된다.
- 익명 엔드포인트이므로 IP 기준 rate limit 필수 (`@nestjs/throttler`).

---

## 8. 프런트 변경 (`Sakwan`)

| 파일 | 조치 |
|---|---|
| `lib/sakwan/onecha-store.ts` | Firestore 호출 → `MOGO_API_URL` fetch로 전면 교체 |
| `lib/sakwan/predict1cha.ts` | 환산·판정 로직 서버 이관, 표시용 포맷터만 잔류 |
| `lib/sakwan/cutoffs.ts` | 하드코딩 컷 제거, 서버 응답 사용 (단일 소스화) |
| `app/1cha/page.tsx` | 계열·성별·수학 선택과목 입력 추가, 총점 모드 축소 |
| `firestore.rules` | **삭제** |
| `firebase.json` | `firestore` 항목 제거 (원복) |

---

## 9. 미확인 항목 — 착수 전 확정 필요

1. **육사 반영 기본점수 과목별 배점 `N`** — 모집요강 PDF 수식 폰트 깨짐. 원문 재확인 필요
2. **해사 1차 선발 척도** — 모집요강 원문 미확보. 표준점수 합산 가정 중
3. **2027학년도 모집요강** — 현재 근거는 육사/국간사 2026, 공사 2025. 올해 요강으로 갱신 필요
4. **모집단위별 추정 컷 실값** — 아래 §10

---

## 10. 컷 데이터 현황

공개 자료에서 확보 가능한 것은 전부 추정치다. 현재 수집된 값:

| 학교 | 구분 | 추정 환산점수 컷 | 출처 |
|---|---|---|---|
| 공사 | 남·인문 | 215~225 | 입시 블로그 추정 |
| 공사 | 남·자연 | 220~235 | 〃 |
| 공사 | 여·인문 | 235~245 | 〃 |
| 공사 | 여·자연 | 240~250 | 〃 |
| 해사 | 남·자연 | 220점대 | 입시 기사 |
| 해사 | 남·인문 | 190점대 | 〃 |
| 국간사 | 자연 | 220점대 | 〃 |
| 국간사 | 인문 | 210점대 | 〃 |
| 육사 | — | 해사보다 높고 공사와 유사 (절대값 미상) | 이데일리 |

추세: 2026학년도 컷은 역대 최저 수준으로, 200점 미만 합격 사례 및 160점대까지 거론됨.
2018년 대비 하락폭 — 육사 남문 47점 / 육사 남이 28점 / 해사 남문 55점 / 해사 남이 66점 /
공사 남문 47점 / 공사 남이 52점 / 국간사 여문 44점.

> **권장**: 학원이 보유한 실제 제보 컷으로 교체. 위 표는 초기 시드로만 사용하고,
> `mg_saagwan_cutoffs`에 `note`로 출처를 남겨 교체 이력을 추적한다.

---

## 11. 일정 관점

1차시험이 D-3이다. 시험 직후 트래픽이 몰리므로 우선순위는:

1. **제출 수집 경로 확보** (POST `/submissions` + 리드 저장) — 데이터는 지금 못 모으면 영원히 못 모은다
2. 환산·판정 정교화 — 데이터가 쌓인 뒤에 개선 가능
3. 컷 실값 교체 — 상시

즉 **수집을 먼저 열고 판정은 뒤따라 정교화**하는 순서가 맞다.
